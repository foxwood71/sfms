# Redis 기본 보안 및 성능 설정
bind 0.0.0.0
port 6379

# 성능 및 데이터 보존
appendonly yes
appendfsync everysec

# ACL 설정은 모두 주석 처리하여 하드코딩된 계정 정보를 제거합니다.
# 이렇게 하면 실행 시 인자로 주어지는 --requirepass 가 정확히 적용됩니다.
# user default off
# user sfms_user on >your_secure_password ~* +@all

# 보안 설정은 래퍼 스크립트에서 환경변수로 주입
